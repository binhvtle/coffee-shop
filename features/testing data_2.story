Feature:
  Testing
  Scenario: auto complete feature
    When User types something like this
    Then User click on a button
    And waiting for page load

    Scenario: auto
      When User types something onetimes again
      Then User ///
      And waitingfff 
      User Click 
    When User types
    And User 
      
